<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="css/mis_solicitudes/pagina_solicitud.css"> <!--Es solo lo de las estrellas-->
    <title>Mis Solicitudes</title>
</head>
<body>
    <%- include('partials/header', {index:0}) %>        <!-- Se incluye el header con la variable index en 0 para que aparezca el boton de inicio -->

    <main class="container text-center bg-body-secondary shadow">
        <h1 class="text-bg-dark rounded mb-4">Información de la Solicitud</h1>

        <table class="table table-striped table-bordered">
            <tbody>
                <tr>
                    <th>Hospital:</th>
                    <td><%= solicitud.NombreHospital %></td>
                </tr>
                <tr>
                    <th>Sección:</th>
                    <td><%= solicitud.NombreSeccion %></td>
                </tr>
                <tr>
                    <th>Fecha:</th>
                    <td><%= solicitud.HoraSolicitud.getDate() + "/" + solicitud.HoraSolicitud.getMonth() + "/" + solicitud.HoraSolicitud.getFullYear() + " " + solicitud.HoraSolicitud.getHours() + ":" + solicitud.HoraSolicitud.getMinutes() %></td>
                </tr>
                <tr>
                    <th>Mensaje enviado:</th>
                    <td><%= solicitud.Mensaje %></td>
                </tr>
            </tbody>
        </table>
        <% if (solicitud.Estado == "pendiente") { %>
            <a class="btn btn-danger" href="<%= '/mis_solicitudes/cancelarsolicitud?idsoli=' + encodeURIComponent(solicitud.IdSolicitud) %>">Cancelar Solicitud</a>
            <h1 class="text-bg-dark rounded mb-4">Respuesta pendiente</h1>
        <% } else { %>
            <% if (respuesta) { %>

            <h1 class="text-bg-dark rounded mb-4">Respuesta Recibida:</h1>

            <table class="table table-striped table-bordered">
                <tbody>
                    <tr>
                        <th>Recepcionista:</th>
                        <td><%= respuesta.Nombre + respuesta.Apellido %></td>
                    </tr>
                    <tr>
                        <th>Respuesta:</th>
                        <td><%= respuesta.Mensaje %></td>
                    </tr>
                </tbody>
            </table>
            <% } else {%>
                <h1 class="text-bg-dark rounded mb-4">No se ha encontrado la respuesta a esta solicitud</h1>
            <% } %>
        <% } %>
    </main>

    <%- include('partials/footer') %>
    
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>