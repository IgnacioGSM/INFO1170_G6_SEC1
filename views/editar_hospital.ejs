<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Hospital - Hospitrack</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <%- include('partials/header', { index: 0 }) %>

    <main class="admin-main container my-4">
        <section id="editar-hospital" class="mb-4 p-4 bg-white rounded shadow" style="max-width: 600px; margin: 0 auto;">
            <h2 class="text-center mb-4">Editar Hospital</h2>
            <div class="card shadow-sm">
                <div class="card-body">
                    <!-- Formulario para editar el hospital -->
                    <form action="/admin/editar_hospital/<%= hospital.idcentro %>" method="POST">
                        <div class="mb-3">
                            <label for="nombre" class="form-label">Nombre del Hospital</label>
                            <input type="text" class="form-control" id="nombre" name="nombre" value="<%= hospital.Nombre %>" required>
                        </div>
                        <div class="row">
                            <div class="col-6 mb-3">
                                <label for="latitud" class="form-label">Latitud</label>
                                <input type="text" class="form-control" id="latitud" name="latitud" value="<%= hospital.Latitud %>" required>
                            </div>
                            <div class="col-6 mb-3">
                                <label for="longitud" class="form-label">Longitud</label>
                                <input type="text" class="form-control" id="longitud" name="longitud" value="<%= hospital.Longitud %>" required>
                            </div>
                        </div>
                        <div class="d-flex justify-content-between">
                            <a href="/admin" class="btn btn-secondary">Volver al Panel</a>
                            <button type="submit" class="btn btn-success">Guardar Cambios</button>
                        </div>
                    </form>

                    <hr class="my-4">

                    <!-- Formulario para eliminar hospital -->
                    <form action="/admin/eliminar_hospital/<%= hospital.idcentro %>" method="POST" onsubmit="return confirm('¿Está seguro de que desea eliminar este hospital?');">
                        <div class="text-end">
                            <button type="submit" class="btn btn-danger">Eliminar Hospital</button>
                        </div>
                    </form>
                </div>
            </div>
        </section>

        <!-- Nueva tarjeta para redirigir a la edición de secciones -->
        <section id="editar-secciones" class="mt-4 p-4 bg-light rounded shadow" style="max-width: 600px; margin: 0 auto;">
            <div class="card">
                <div class="card-body">
                    <h3 class="card-title text-center mb-3">Gestionar Secciones</h3>
                    <p class="card-text text-center">Administra las secciones asociadas a este hospital.</p>
                    <div class="text-center">
                        <a href="/admin/editar_seccion/<%= hospital.idcentro %>" class="btn btn-primary">Editar Secciones</a>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <%- include('partials/footer') %>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
